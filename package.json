{
  "name": "unredact-web-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "playwright:install": "playwright install chromium",
    "test:web-ui-benchmark": "playwright test web/e2e/web_ui_batch_benchmark.spec.mjs --reporter=line",
    "test:web-ui-benchmark:headed": "playwright test web/e2e/web_ui_batch_benchmark.spec.mjs --headed --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
